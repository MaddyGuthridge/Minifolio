/**
 * Test cases for getting item info
 */

import { describe, it } from 'vitest';

describe('Success', () => {
  it.todo('Correctly returns info');
});

describe('400', () => {
  it.todo('Gives an error if data is not set up');
})

describe('404', () => {
  it.todo("Rejects when an item doesn't exist");
});
